<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fzu.stop.dao.FeedbackDao">
    <select id="getFeedbackList" resultType="com.fzu.stop.pojo.FeedbackDO">
        select id, title, content, user_nickname username, status_id status, submit_time submitTime
        from feedback
        limit #{start},#{lim};
    </select>

    <select id="getCount" resultType="java.lang.Integer">
        select count(*) from feedback;
    </select>

    <select id="getFeedback" resultType="com.fzu.stop.pojo.FeedbackDO">
        select id, title, content, user_nickname username, status_id status, submit_time submitTime
        from feedback
        where id = #{id};
    </select>

    <update id="updateFeedback">
        update feedback
        set status_id = #{status}
        where id = #{id};
    </update>

    <insert id="addFeedback" parameterType="com.fzu.stop.pojo.FeedbackDO">
        insert into feedback(user_nickname, open_id, content)
        values (#{username}, #{openId}, #{content});
    </insert>

</mapper>   